rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Kurikulum Merdeka E-Rapor system
    // Open read and write for educational applet records
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
